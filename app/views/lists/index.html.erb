<h1>To Do List</h1>
<p><%= link_to 'New Chores', new_list_path %></p>
<% @lists.each do |list| %>
  <h2><%= list.chore %></h2>
  <!-- <h3><%= list.description %></h3> -->
  <%= link_to 'Edit Chores', edit_list_path(list)%>
  <%= link_to 'Remove Chore', list_path(list), method: :delete %>
  <%= link_to 'Show Chore', list_path(list) %>
<% end %>
